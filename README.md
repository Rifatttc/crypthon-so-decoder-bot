# 1. GitHub এ নতুন repository তৈরি করুন
# নাম দিন: crypthon-so-decoder-bot

# 2. Local repository initialize করুন
git init
git add .
git commit -m "Initial commit: Crypthon & SO Decoder Bot"

# 3. GitHub এ push করুন
git remote add origin https://github.com/YOUR_USERNAME/crypthon-so-decoder-bot.git
git branch -M main
git push -u origin main
